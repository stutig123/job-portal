name: Deploy Job Portal

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to EC2 with Docker & Ansible
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: stutigambhir
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image - Backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: stutigambhir/job-portal-backend:latest

      - name: Build and push Docker image - Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: stutigambhir/job-portal-frontend:latest

      - name: Run Ansible Playbook
        uses: dawidd6/action-ansible-playbook@v2
        with:
          playbook: ansible/deploy.yml
          inventory: ansible/inventory.ini
          key: ${{ secrets.STUTI_PRIVATE_KEY }}
          options: |
            --extra-vars "dockerhub_user=stutigambhir"
#hello